#!/usr/bin/env node

import { WapWordAnalyzerCLI } from './cli';

/**
 * Entry point for the Word Analyzer CLI application.
 * This script is intended to be executed as a
 * command-line interface (CLI) tool.
 */
new WapWordAnalyzerCLI();
