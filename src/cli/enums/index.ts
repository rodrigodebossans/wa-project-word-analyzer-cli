/**
 * Enum representing various metric labels used in the Word Analyzer CLI.
 */
export enum WapMetricLabels {
  PARAMETER_LOADING_TIME = 'parameter-loading-time',
  PHRASE_VERIFICATION_TIME = 'phrase-verification-time',
}
